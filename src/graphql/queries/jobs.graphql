query Jobs($limit: Int, $cursor: String) {
  jobs(limit: $limit, cursor: $cursor) {
    hasMore
    jobs {
      id
      creatorId
      title
      descriptionSnippet
      createdAt
      updatedAt
    }
  }
}
